import 'package:flutter/material.dart';

class LayoutTab extends StatelessWidget {
  const LayoutTab({super.key});

  @override
  Widget build(BuildContext context) {
    print("LayoutTab");
    return Center(child: Text("LayoutTab"));
  }
}
